# 16 — Gaps in Test Strategy

**Status:** the test-coverage companion to the integration board (`00-INTEGRATION-BOARD.md`).
No code changes — this names *where the existing test strategy does not yet cover the
rollout*, so each gap becomes a base exit-gate criterion rather than a surprise later.

**The structural reason there are gaps:** aOa's current test contract is **behavioral
parity against Python** (fixtures in `test/fixtures/` are the source of truth, per
CLAUDE.md). The entire arch layer (E1–E7, P1–P6) is **net-new — there is no Python to
parity against.** So the contract shifts from *parity* to **determinism + golden fixtures
+ invariants + benchmarks + one safety test**. Every gap below is an instance of that shift.

**Gap type legend:** 🟥 blocker (a base cannot pass its gate without it) · 🟧 important ·
🟦 hardening. **Mapped to the base it gates.**

---

## The gap ledger

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T1 | **G0 keystone benchmark** — the ≤+3% build budget is *asserted, never measured*. No build-time benchmark of parse-with-edges vs without exists. **STATUS (F0, 2026-07-02): MEASURED — GREEN.** L19.6 spike on kubernetes @ `efab4fac` (24,194 files, 5+5 runs): p50 Δ −4.51% (verdict doc), −15.53% on fresh re-execution — spike cost lost in run noise, decisively ≤+3%. Verdict: `details/benchmark-verdict-2026-07-02.md`. Re-run required at L19.13 with the T28/T29 hardening below. **STATUS (F1, 2026-07-02): re-run GREEN as parse-cost evidence only** — L19.13 re-run on kubernetes @ `efab4fac`, 4 interleaved pairs, p50 Δ −6.09%, edges=100,914 asserted every ON run (`details/benchmark-verdict-F1-2026-07-02.md`). **Still-gap: the measured scope is hollow for what ships** — the flag-on boot path persists edges one bbolt tx/file (unmeasured, est. ~30s on the corpus — see T31) and flag-on/off index divergence contaminates the A/B premise (T30). Re-run again per checkpoint-F1 P4 with those fixed. **STATUS (F1 re-entry, 2026-07-02): P4-widened re-run GREEN — CLOSED for Base 1.** ON arm now = parse + extract + §2.4 resolve + group + `ReplaceAllEdges` persist to real bbolt; 4 interleaved pairs with **alternating** order (T28 residual cleared), Δ **+0.06%** p50 (OFF `1m9.749s` / ON `1m9.792s`, gate ≤+3%), `edges=100914 persisted=100914` every ON run, 0 every OFF. Governing log: `details/t1-rerun-F1-2026-07-02.log` (supersedes the 2-pair −3.64% quick-confirm in the verdict doc — fold in per checkpoint PC6). | 🟥 | 1 | G0 | Benchmark `BuildIndex` before/after the import-extraction concern on a large real repo (not a no-op baseline). **This is the single make-or-break test** — it decides whether E1 rides the always-on parse or falls back to opt-in/async. |
| T2 | **Edge-store per-file invalidation** — keyed-by-file delete (`Delete(fileIDKey)`) must drop exactly one file's edges with no orphans, in O(edges-for-file). No `store_test` covers edges. **STATUS (F1, 2026-07-02): COVERED/PASSING** — `TestEdgeStore_T2_PerFileDeleteRoundtrip` + 16 EdgeStore tests green. Residual: watcher-delete path only; reindex-after-shrink orphans are NOT covered → T34. | 🟥 | 1 | G0/G4 | Save edges for a 3-file repo; delete one file's key; assert the other two intact, the deleted file's gone, and the layout never full-scans. JSON round-trip parity beside `store_test.go`. |
| T3 | **Freshness / ETag regression** — today a save reindexes but does **not** bump the ETag (`bumpRevision` absent from `onFileChanged`/`Reindex`); the viewer serves a stale 304. No test guards this. **STATUS (F1, 2026-07-02): COVERED/PASSING** — revision bumps on all mutation paths incl. zero-symbol + delete + `Reindex`; stale `If-None-Match` → 200 (`TestETag_StaleRevision_Returns200`). | 🟥 | 1 | G0/G6 | Edit a file (incl. one yielding **zero symbols** — the path the prior draft missed) → `revision` increments same tick → next `GET` with old `If-None-Match` returns **200 + new ETag**, not stale 304. Pattern on the existing ETag test. |
| T4 | **Determinism / golden shards** — no Python parity exists for renditions; nothing asserts "same repo → same edges/shards." **STATUS (F2, 2026-07-03): COVERED/PASSING within ruled scope** — 12/12 green (`TestManifestGolden`, `TestDeterminism_*`, `TestContentHashStability`, `TestG7_SourcesPresent`); sha256[:12] content hash (`marshal.go:34-36`); goldens = component/dsm/cycles + manifest, stamped `prov:"derived"` (D1) with `sources:[{file,line}]`. **Scope ruling (checkpoint-F2 R1):** entity/simple **shard** goldens bind L19.23's T4 (F2 built + tested entity/simple *captions* only); kickoff §6's "all five shard kinds" phrasing superseded by the board's views-lit scope. Residual: no golden covers an ext-target unit (→ T50). **STATUS (V1-A, 2026-07-14): EXTENDED/PASSING** — goldens regenerated for the A1–A3 fields (`layer`/`ico`/`inferred` + caption `count`→`findingsClause` split); delta verified intended-fields-only vs `13162fb`/`3d1da45` (edges/members byte-identical); `TestRoleFor` (26 cases) + `TestBucket_InferredFalseSurvivesSerialization` (omitempty trap pinned) green. | 🟥 | 2 | G1 (reframed) | Byte-stable golden fixtures: each renderer on a fixed edge-set emits deterministic shard JSON with a **stable content hash**; re-run yields byte-identical hash (so the ETag/304 short-circuit holds). Replaces parity for this layer. |
| T5 | **Detector correctness fixtures** — Tarjan/god/orphan have no test fixtures. **STATUS (F2, 2026-07-03): COVERED/PASSING** — 16/16 green (cycle, god-node, orphan, `AllFire_Fixture`, budget overflow ±, dead-code candidate ±nil, DSM mutual pair ×3, Tarjan ×5), all `file:line`-stamped. Residuals: min-count **cut edge** never discriminated (cycle fixture uses all `Count:1`, cheapest-edge selection untested); dead-candidate green at domain level while production wiring passes **nil** refHits (→ T46, punch PC3). | 🟧 | 2 | G0 | On a fixture with a known 3-module cycle + one god-node + one orphan: assert Tarjan returns exactly that SCC, degree detector flags the god-node, orphan detector flags the isolated file — all `file:line`-stamped, all deterministic. |
| T6 | **graph→peek handoff + import-edge asymmetry** — the load-bearing claim that a graph result resolves to a peekable body (and that import-edge *targets* resolve to package, not body) is unverified by any test. **STATUS (F2, 2026-07-03): COVERED/PASSING** — `TestT6_Derive_Peek_Handoff`, `_ImportEdge_Asymmetry_Ext` (ext target → package grain, honestly stamped), `_ViewShard_ByteIdentity`, dead-daemon shim `_EmitsError`+`_LazyRevives` (gotcha 3b both directions), guidance block live in `init.go`. Live demo transcript in checkpoint-F2 §1 (derive → facts → grep → `peek 4ss3q` full body). Residuals: helper double-`init`s to work around the boot-derive hole (→ T45, punch PC1); no standing automated `aoa peek`-body assertion (optional hardening, ruling R3). | 🟧 | 2 | G3 | Assert a resolved intra-repo edge peeks **both** ends to a method body via `TokenRef`→`Metadata`→`StartLine-EndLine`; assert an external import edge peeks the **source site** but resolves the target to package grain (honestly stamped), not a phantom body. (Design in `13`.) |
| T7 | **MCP-vs-CLI contract parity** — the 4-tool MCP surface must return results identical to the matching `aoa arch` CLI call; no contract test. **STATUS (F2, 2026-07-03): N/A — no MCP surface exists** (zero MCP code; protocol stays six `MethodArch*` + CLI-only reach/blast aliases per the 2026-07-02 ADR). T7 binds **L22.6** when pulled, not Base 2. | 🟧 | 2 | G3 | Assert the MCP server enumerates **exactly 4** tools; each returns a `file:line:commit` result byte-equal to its CLI sibling; blast-radius reflects a freshly git-changed file with no stale graph. |
| T8 | **`aoa arch` three-mode parity** — like `grep`/`peek`, must give consistent output/exit codes in direct/pipe/daemon modes. No test. **STATUS (F2, 2026-07-03): STILL-GAP despite green suite — vacuous as executed.** 16/16 tests pass, but with zero arch data every parity assertion reduced to exit-1 == exit-1: data parity is gated `if hasData && …` (`arch_cli_test.go:214`) and `hasData=false` is a log line, never a failure; Facts/Derive/Findings modes compare exit codes only; two environmental `t.Skip` escapes. Root cause = boot-derive gap (T45). Binds Base-2 Tested via checkpoint-F2 PC1+PC2: with data present, assert shard-**byte** identity across daemon / direct-RO / dead-daemon and make `hasData=false` fail. **STATUS (F2 re-verify, 2026-07-03): COVERED/PASSING** — de-vacuated @ `0be8cb8`: byte parity asserted live across all three modes (views 502B · findings 320B · facts 161B · derive 39B), `hasData=false` → `t.Fatal`, env skips removed. | 🟦 | 2 | G3 | Run each `aoa arch *` subcommand in all three modes; assert identical output + exit codes; the unknown-method fallback (`server.go:246`) stays untouched for non-arch methods. |
| T9 | **Blind-judge gate on DERIVED data** — the gate exists but runs against the **mock/proxy** generator, not real derived shards. **STATUS (F3, 2026-07-03): PARTIAL — 3/4 PASS on real DERIVED shards, still-gap on code.** Executed per rulings U-2/R3 (headless chromium on the daemon-served `/arch/` fed by real go-cmp shards; blind judge answered component/dsm/cycles from the image alone, REAL badges visible; PNGs at `/home/corey/t9-screens/`). **code: RENDER FAILURE** — `viewer.js:350` `lblW` crashes on label-less code edges (→ T54, punch PF1); estate label contaminated the imagery (→ T57, PF7). Re-capture code + relabeled estate at the re-verify. **STATUS (V1-A, 2026-07-14): STILL-GAP — evidence invalidated.** Gate screenshots captured on 2 of 3 labs serving **stale pre-phase shards** (checkpoint live shard-byte probe: shorty missing `inferred` = pre-A3, cobra missing `layer`/`ico`/`inferred` = pre-A1; only gin current); the serving-current probe checked viewer.js, not shard bytes; no blind-judge verdicts recorded; cobra is not a true stranger repo; cycles/dsm/code uncaptured after the 736-line viewer rewrite. Binds V1-A Tested via punch PA1 (+ root cause T64). Record: `checkpoint-V1-A.md`. | 🟥 | 3 | G6 | Port the blind judge (`MODEL-STANDARD.md:43-53`) to screenshot the **daemon-served** `/arch/` URL fed by real derived shards; a view passes only if the judge answers its question from the image alone. (Currently mock-only.) |
| T10 | **Touch-one-package proof (the integration exit gate)** — no end-to-end test proves incremental correctness on a stranger repo. **STATUS (F3, 2026-07-03): COVERED/PASSING within the U-1 ruling** — stranger repo pinned + recorded (ruling R2: `google/go-cmp` @ `v0.6.0` / `c3ad843`, multi-package); init → daemon → edit `cmp/cmpopts/equate.go` → dsm `aea53105→1a02075d` (137→138 deps), component `5707dae0→1a419c57`, code changed (edit in its entrypoint pkg) — all genuinely affected; **cycles `6f6807dc7b39` byte-stable**; stamps at §6 ceilings (3× `prov:"derived"` REAL + code mixed). Residual: the U-1 "code if landed" clause re-binds after PF1/PF6 change code-shard bytes — re-check that line at the re-verify. | 🟥 | 3 | G0/G6 | Clone a stranger repo → `aoa init` → 4 views render REAL-stamped → edit **one** package → assert **only the affected shards change** (hashes of untouched scopes stable). This is the L19.5 exit gate. |
| T11 | **The self-test invariant as CI regression** — aOa's own `domain → adapters` edge set should be **empty** (hexagonal). Proposed, never wired. **STATUS (F2, 2026-07-03): COVERED/PASSING** — `TestArchSelftest` green on aOa itself: 21 units, 66 dep edges, 0 domain→adapters violations, 0 SCCs. Residual: the hexagonal half is a hand-rolled loop rather than an arch detector (the cycles half does use `DetectCycles`) — cosmetic, standing regression is live. **STATUS (V1-A, 2026-07-14): re-run PASSING** — 22 units, 75 dep edges, 0 domain→adapters violations, no cycles. | 🟧 | 2/3 | G4 | A CI test that runs the cycle/boundary detector on aOa itself and asserts the domain→adapters violation set is empty — arch eating its own dog food as a standing regression. |
| T12 | **Leash safety test (CRITICAL)** — nothing asserts the service **never** initiates an LLM call on a derive path, or that the overlay loader rejects invented ids. | 🟥 | 4 | scope-law/G0 | Assert: (a) no derive path makes a network/LLM call (the model runs only in the outside CLI/MCP layer); (b) the overlay loader **rejects any id not in the facts** with a warning fact and drops the view to **MIXED**, never REAL. The single most important *safety* test in the plan. |
| T13 | **Diff renderer golden fixtures** — Mode A/B before/after diffs have no fixtures. | 🟧 | 4 | G1(reframed) | Golden before/after edge-sets → deterministic delta (new cycles / blast / new findings) from pure set arithmetic; assert no LLM in the path; conformance edge classes (convergent/divergent/**absent**) render with the existing `e.tag` machinery. |
| T14 | **Conformance classification** — declared-vs-derived diff (`arch.yaml`) is untested. | 🟦 | 4 | G6 | On a declared pattern + a known-divergent repo, assert correct convergent/divergent/absent edge classification and baseline/freeze "report only NEW drift" semantics. |
| T15 | **Scale / perf regression for arch** — `scale_gauntlet` covers search, not arch. Max-RSS-during-compact and sub-ms arch reads on a 30k-file fixture are ungated. **STATUS (F2, 2026-07-03): PARTIAL — latency half green, RSS/read halves still-gap.** 30k-unit/90k-dep fixture built; derive latency asserted 2.63s ≤ 10s; C2 gated (`TestArchDerive_C2BurstDebounce` — one re-render per debounce window); `BenchmarkRenderAll_30k` 2.60s/op, 774MB/op. Still-gap (punch PC5): max-RSS **logged** (71.8–93.9MB growth) but never asserted against a budget; header comment says 5s while the constant asserts 10s (undocumented doubling); "arch reads stay sub-ms" / L19.16's "<1ms daemon path" measured nowhere (nearest is T18-b's edge-read P99 — a different path); real-app-path compact RSS + shard write amplification unmeasured (domain-level fixture only). | 🟧 | 2/3 | G0 | Extend the gauntlet: assert arch reads stay sub-ms and **max-RSS-during-compact** stays within budget on the 30k-file / 1.2M-symbol fixture; edge memory grows ~linearly. |
| T16 | **Vendored-bundle fork-guard** — the viewer extracted to `static/arch/` must not drift from the source generator, and the offline ESM bundle must stay in budget. **STATUS (F3, 2026-07-03): COVERED/PASSING as implemented** — `TestT16BundleBudget`: gz 544,574 B (532 KB ≤ 650 KB), raw 1,785,164 B (≤ 2.2 MiB), zero esm.sh/CDN imports asserted in Go; `TestViewerForkGuard` (rewritten post-`__VIEW_INTENT__`) + `TestStandardsCopiesInSync` green. Residual: guard weaker than this row's spec — head/tail 512-byte substrings of viewer.js only, `t.Skipf` when the mockup is absent, index.html/viewer.css unguarded (→ T56, hardening PF8). **STATUS (V1-A, 2026-07-14): budget half PASSING** (`TestT16BundleBudget` green after the +736-line viewer rewrite); **fork-guard half hollow — T56 realized and escalated to binding** (viewer.js vs mockup ~95% divergent — identical prefix 7,990 of 151,764 bytes — while the guard stays green). Binds V1-A Tested via punch PA4. | 🟦 | 3 | G0/maintenance | Hash-compare test between the extracted `viewer.js/html/css` and the generator source (fork-guard); assert the vendored esbuild bundle drops the esm.sh CDN imports and stays ≤2.2MB raw / ≤650KB gz (elkjs dominates ~1.5MB). |

**Added by the integration-pattern validation swarm (2026-06-21 — see board §0):**

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T17 | **Lock-ordering assertion** — no `db.Update` ever runs while `App.mu` is held (arch OR index path). The existing `markIndexDirty`→`SaveIndex`-under-mutex pattern (`app.go:838-846`) must be **fixed, not copied**. **STATUS (F1, 2026-07-02): STILL-GAP despite green suite** — harness exists and passes at `-count=1`, but (a) `TestT17_Race` is a reproducible false positive at `-count=50` (TryLock heuristic detects "held by anyone", not "held by caller"), and (b) the harness never drives the session-signal autotune paths, where two REAL C1 violations survive (`observer.go:142/182` — proven with the phase's own lockGuardStore). See T32; re-opens L19.8 per checkpoint-F1. **STATUS (F1 re-entry, 2026-07-02): COVERED/PASSING** — caller-owned lock detection (`newRaceApp` noopStore); the two session-autotune paths + `SaveAllDimensions` now guarded; `-race -count=50` green (T32 discharged). Recorded residuals (hardening, not re-opens): concurrent C1 coverage is zero by design (noopStore is sequential-only — a violation reachable only concurrently is uncatchable); `TestT17_SaveAllDimensionsNotUnderMu` is vacuous (drives no production path); the autotune guards don't assert autotune fired (silent-vacuousness risk, the promptN=49 class). See checkpoint-F1 R5/PC4. | 🟥 | 1 | G0/G4 | Instrument or wrap the store so any `db.Update` entered with `a.mu` held fails the test; run the watcher + search paths through it. |
| T18 | **Burst write-pressure test** — combined write wall during a multi-file event storm is unbudgeted (each writer only budgeted in isolation; bbolt is single-writer). **STATUS (F1, 2026-07-02): COVERED/PASSING at unit level** — 200 events → exactly 1 batch tx; P99 reads <1ms asserted; race-clean. Residual weaker-than-AC assertions (wall-clock dropped, mock-store reads, timer path unexercised) → T39. | 🟥 | 1 | G0 | Simulate a branch switch (~200 file events <1s): assert total write-tx wall bounded, per-window coalescing works (one tx per debounce window), and reads stay sub-ms *during* the burst. |
| T19 | **Both-direction schema migration** — new buckets (`edges`, `arch_shards`, baselines) have no `_version` byte; old-binary-opens-new-DB after rollback is undefined (index-wipe risk). **STATUS (F1, 2026-07-02): COVERED/PASSING on the write path** — 5/5 both-direction tests green (`_version` written+checked, drop-and-recreate, no index wipe). Residual: read path ignores `_version` and swallows unmarshal errors → T38. **STATUS (F2 sweep, 2026-07-03): EXTENDED/PASSING** — both buckets added since Base 1 covered both directions: `ArchStore_T19_*` ×5 (**`arch_shards`**) + `FindingsStore_T19_*` ×5 (**`facts_findings`**) — version byte on create, mismatch drop-and-recreate, new-binary-old-DB, old-binary-new-DB, wrong-version read — 31/31 green with the EdgeStore set. | 🟥 | 1 | G4/data-safety | Open (i) a pre-arch DB with the new binary and (ii) a post-arch DB with the prior binary: both degrade gracefully (missing bucket = empty; version mismatch = drop-and-re-derive), **never** trip index self-recovery. |
| T20 | **`-race` soak** — new shared mutable state (shard cache, adjacency cache) read lock-free by socket/web while the watcher invalidates and compact rewrites; zero race coverage today. **STATUS (F3, 2026-07-03): STILL-GAP — the soak was never authored.** Zero code matches this row (`grep -rln T20 --include=*.go` = zero hits); the kickoff §5 authoring step never landed, so the L19.21 gate row had nothing to run. Nearest coverage green (`-race` ×4 across app/web/socket/bbolt/arch; `TestArchDerive_RaceWithIndexSwap -race -count=50` — but that is serial derive-vs-swap, not concurrent reads vs `SaveShards`/`SaveManifest`). One unreproduced `internal/app` `-race` FAIL on a combined run (5 retries green) is exactly what this soak would instrument. **Punch PF3 — binds F3 Tested.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `813862b` — `TestT20_RaceSoak`: 100 concurrent derives + 4 readers + compactor vs real bbolt, `-race` clean, 0 torn reads, 2.07s on the final tree. **STATUS (V1-A, 2026-07-14): re-run PASSING** — 2.10s `-race` clean @ `5dd0122`. | 🟧 | 3 | G0 correctness | N seconds of concurrent socket reads + watcher writes + forced compacts under `go test -race`: zero races; every served shard is a consistent snapshot (no torn read across a revision bump). |
| T21 | **Fuzz the trust boundary** — the overlay loader and JSONL fact reader parse *untrusted model-generated JSON* inside the live daemon; security-relevant, unfuzzed. | 🟧 | 4 | leash/security | Go fuzz targets for the overlay parser + fact reader: no panics, invented ids always rejected, malformed input never mutates state. |
| T22 | **Property-based graph invariants** — golden fixtures catch regressions, not classes of bugs. **STATUS (F2, 2026-07-03): PARTIAL** — 4/4 green: `ByteStability_UnderPermutation`, `MemberMapsToRealUnit`, `DSMMatchesEdgeSet`, `CyclesSubsetOfSCCs` (the kickoff-§6 gated slice). Still-gap: one seeded permutation only, and the **incremental-equivalence property ("N single-file edits ≡ one full rebuild") has zero tests repo-wide** — the exact property the T41 watcher-resolver fix was meant to restore. Hardening, carried (does not re-open Base 2). | 🟦 | 2 | G1(reframed) | Properties: every edge endpoint resolves to a live node; cycles ⊆ SCCs; DSM = adjacency of the edge set; N single-file edits ≡ one full rebuild (incremental-equivalence). |

**Added by the L21 phase checkpoint (2026-07-02 — see `checkpoint-L21.md`).** L21 (daemon
resilience) is operational hardening outside the arch matrix: **none of T1–T22 applied**;
its exit gate was the phase-specific E-1…E-9 set (kickoff-L21 §6) — all 9 PASS on live
evidence, verdict **Conditional** pending T23. The checkpoint panel exposed these NEW gaps
("Base" = the L21 surface, not a rollout base):

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T23 | **E-2 flock single-spawn not locked by the suite** — `TestDaemonEnsure_RevivesOnce` computes the pgrep count but asserts only health-exit-0 (a double-spawn passes); it also calls `runAOA` (`t.Fatalf`) from spawned goroutines. Live storm proved one spawn; the suite doesn't. | 🟥 | L21 | G0/G4 | Assert exactly one spawn deterministically (count "daemon starting" lines in `.aoa/log/daemon.log`, not pgrep); route goroutine errors via channel to the test goroutine. **Punch P2 — gates L21 Tested.** |
| T24 | **Revive-failure path untested** — `reviveDaemon` takes `LOCK_EX` with no `LOCK_NB`/timeout/backoff; if `.aoa` exists but the daemon can't start (corrupt DB, port conflict), callers queue serially at ~15s each while the status-line hook enqueues ~1/s → unbounded process pileup. Only the happy path has evidence. | 🟧 | L21 | G0/G6 | Simulate an unstartable daemon; assert ensure exits fast when another reviver holds the lock (`LOCK_NB`) and repeated failures back off — no queue growth over N hook ticks. |
| T25 | **Version-skew guard has zero coverage** — no test feeds a `DaemonOK`-less (old-daemon) health response through `runHealth`; the derive predicate (`ok\|\|recovered`) is duplicated server-side (`server.go:309`) and client-side (`health.go:50-57`) and can silently drift. | 🟧 | L21 | G3 | Feed an old-daemon JSON payload through the CLI health path; assert tri-state derives from `Status` both directions; extract one shared helper so the predicate exists once. |
| T26 | **Live-daemon Web probe is a startup snapshot** — `webOK := a.WebServer.Port() > 0` (`app.go:497`) is true forever after bind; a died HTTP listener still reports `Web: up`. Only the dead-daemon path actually dials. | 🟧 | L21 | G6 | Kill the web listener under a live daemon → `aoa health` reports `Web: down` (probe closure performs a real 200ms localhost dial, like the dead path). |
| T27 | **L21 assertion-tightening grab-bag** — `TestGrep_DeadDaemon_Revives` never asserts semantic stdout (revive-then-empty passes); stale-status tested only at 3h, not the 60s boundary; `aoa remove` vs hook-backgrounded `ensure` race can resurrect `.aoa` mid-removal (no intentional-stop marker); wedged-tailer staleness under a live daemon unflagged. | 🟦 | L21 | G3/G6 | Add `strings.Contains(stdout, "hello")` to the revive test; test the 60s threshold edge both sides; test that `remove` completes with the hook firing (no `.aoa` resurrection); consider a stop-marker. |

**Added by the F0 phase checkpoint (2026-07-02 — see `checkpoint-F0.md`).** F0's gated set
was T1 (GREEN, see status in the T1 row) plus the L19.7 spec-amendment ACs — all executed
gates PASS; verdict **Conditional** on punch P1–P3 (spec-text ADR propagation + committing
the phase's evidence). The panel exposed two NEW gaps in the T1 harness itself, binding on
the **L19.13 T1 re-run** (they do not re-open the F0 verdict):

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T28 | **T1 harness lacks statistical power for the gate it enforces** — the F0 protocol ran all-baseline then all-spike sequentially; baseline spread was 72.9–91.3s (~±12%) against a +3% gate, so inter-block drift alone exceeds the gate width and a true +3% regression could pass green. The verdict doc also preserved only p50 + range, not the per-run raws. **STATUS (F1, 2026-07-02): SATISFIED** — L19.13 re-run interleaved 4 (off,on) pairs, full per-run raw table + memory headroom recorded in `details/benchmark-verdict-F1-2026-07-02.md`. Residual bias: OFF always ran first in each pair (warmer cache for ON) — alternate order at the P4 re-run. | 🟥 | 1 (L19.13) | G0/G8 | Interleave/randomize A-B arms per iteration (or discard a warm-up run); record the full per-run raw table in the verdict doc; record memory headroom (F0 hardware was 12GB indexing kubernetes — swap-induced variance risk). |
| T29 | **T1 harness never asserts the spike arm did work** — `spikeImportCount` was written but never asserted/logged; the kickoff §6 ">50K imports processed" precondition went unevidenced (the arm was verified real only by an out-of-band panel test: flag ON → 3/3 imports extracted). **STATUS (F1, 2026-07-02): SATISFIED** — harness asserts edges>50K every ON run (100,914 on the pinned corpus) and 0 every OFF run; counts logged in the verdict doc. | 🟧 | 1 (L19.13) | G8 | The benchmark fails unless the spike arm reports extracted-import count > 50K on the pinned corpus (and 0 with the flag off); log the count into the verdict doc. |

**Added by the F1 phase checkpoint (2026-07-02 — see `checkpoint-F1.md`).** F1's gated set
was T1(re-run)/T2/T3/T17/T18/T19 + C4 dark check + proof edge + `make check`; verdict
**Failed** — 9/11 gate lines green, but `make check` fails at lint, the L19.9 §2.4 resolver
is unimplemented, two live C1 violations survive on paths the T17 harness never drives, and
two findings (T30/T31) hollow out T1's end-to-end claim. Re-opens L19.8 + L19.9; full gate
re-run required per checkpoint-F1 P4. The panel exposed these NEW gaps:

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T30 | **Flag-on/off index equivalence untested** — C4 promises "flag flips edge emission only", but flag-on `BuildIndexWithFacts` drops content-line tokens for symbol-less parseable files (`indexer.go:169-193` `continue`s on `parseErr == nil` regardless of `len(metas)`; flag-off and watcher paths keep them). The ON index diverges from OFF — also contaminates T1's A/B premise. | 🟥 | 1 (re-run) | G0/C4 | After the fix: build the same fixture corpus flag-on and flag-off; assert token maps + symbol metadata byte-identical (edges are the only permitted delta). Include a parseable zero-symbol file in the fixture. **STATUS (F1 re-entry, 2026-07-02): COVERED/PASSING, weaker than this row** — `TestT30_FlagOn_ContentEquivalence` green (incl. zero-symbol file), but it compares token/ref **counts** + metadata length, not byte-identical maps — a count-preserving divergence passes. Strengthen to byte-equality (hardening). |
| T31 | **T1 scope hollow — flag-on boot persistence unmeasured and unbatched** — the harness times `BuildIndexWithFacts` only; the real boot path (`WarmCaches` `app.go:877-884`, `Reindex` `app.go:3089-3095`) persists one bbolt tx per file. Measured: 1.58ms/tx; 1000 files = 1.58s vs one `SaveEdgesBatch` = 6.2ms (257×); est. ~30s on kubernetes — blows +3% end-to-end, default-on. | 🟥 | 1 (re-run) | G0 | Wire bulk persists through `SaveEdgesBatch`; extend the T1 harness (or add a boot benchmark) to time flag-on cold start **including edge persistence**; gate stays ≤+3% end-to-end. **STATUS (F1 re-entry, 2026-07-02): SATISFIED** — `WarmCaches` (`app.go:907`) + `Reindex` (`app.go:3196`) wired through `ReplaceAllEdges`; the widened T1 times flag-on boot **including** persistence: Δ +0.06% p50. |
| T32 | **T17 harness unsound + incomplete** — `TestT17_Race` false-positives reproducibly (`-race -count=50` FAIL: TryLock detects "held by anyone", not "held by caller"); `t.Fatalf` from a spawned goroutine (T23 class); harness never drives the session-signal autotune paths where two REAL C1 violations live (`observer.go:142/182`, proven via lockGuardStore: `promptN=49` + one `EventUserInput` / one Grep `EventToolInvocation`); `SaveAllDimensions` unguarded. | 🟥 | 1 (re-run) | G0/G4 | Make lock-held detection caller-owned; route goroutine failures via channel; add the two session-signal autotune paths + `SaveAllDimensions` to the guard; `-race -count=50` stays green. **STATUS (F1 re-entry, 2026-07-02): SATISFIED** — all four repairs landed; `-race -count=50` → ok 31.3s. Ratified trade-off: caller-owned detection narrowed the guard to sequential scope (no concurrent C1 coverage) — recorded, see T17 status. |
| T33 | **`FromFile` relative-path invariant untested** — port contract (`ports/facts.go:7-8`) says project-relative, never absolute (G7); batch path rewrites (`indexer.go:190`) but the watcher persists absolute (`watcher.go:149,157`) → mixed keyspace, breaks F2 resolution. | 🟧 | 1 (re-run) | G7/G4 | Property test over both write paths: every persisted `ImportEdge.FromFile` is relative (no leading `/`, no `..`); watcher-written edge for an absolute-path event round-trips relative. **STATUS (F1 re-entry, 2026-07-02): COVERED/PASSING** — `TestT33_WatcherFromFileRelative` green; proof run asserted zero absolute paths across 1,464 edges. Note: asserts `FromFile` form only — the same mixed-keyspace defect on `ImportPath` (watcher path unresolved) is **T41**. |
| T34 | **Stale-edge orphans across Reindex/WarmCaches** — no bucket clear/replace-all; fileIDs reassigned per rebuild → deleted/renumbered files leave phantom rows in `LoadAllEdges` (F2's renderer input). T2 covers watcher-delete only. | 🟧 | 1 (re-run) | G0/G7 | Reindex-after-shrink test: index N files, persist edges, delete a file + rebuild → `LoadAllEdges` returns zero edges for the dead file and no fileID-collision ghosts. Needs an EdgeStore reset/replace-all op. **STATUS (F1 re-entry, 2026-07-02): COVERED at store level** — `ReplaceAllEdges` `ClearsStale`/`EmptyClears`/`Atomic` green. Residuals: no app-level reindex-after-shrink e2e by name; `facts_unresolved` is never reset (→ T42); files deleted while flag-OFF leave phantom rows served after re-enable until a manual Reindex (no auto-trigger; recoverable via C3). |
| T35 | **Learner snapshot escapes the mutex** — `a.Learner.State()` returns the live pointer (`learner.go:101`); `app.go:735` marshals it outside `a.mu` → torn-JSON persistence race; `-race` green only because no test runs concurrent autotune. | 🟧 | 1 (re-run) | G5/G4 | Deep-copy (or marshal under lock); add a `-race` test running concurrent search-observe + autotune-triggering session events through the save path. **STATUS (F1 re-entry, 2026-07-02): PARTIAL** — fix landed (`LearnerState.Clone()` at `ports/storage.go:146`, deep-copy under lock in both observer paths); the dedicated concurrent-autotune `-race` test is still absent. Folded into this remit: `WarmCaches` still passes the **live** `a.Index` to `SaveIndex` un-Cloned outside the lock (`app.go:901`, pre-existing); racing autotunes can persist an older clone out of order (self-healing at next autotune/Stop — accepted residual). **STATUS (F1 light re-verify, 2026-07-03): COVERED/PASSING** — `TestT35_ConcurrentAutotune_Race` landed (`4addf8c`, -race clean, asserts autotune fired); `WarmCaches` Clones `a.Index` under lock before `SaveIndex` (`566689c`). Remaining accepted residual: out-of-order clone commit window (self-healing). |
| T36 | **C4 predicate split-brain** — `cmd/aoa/cmd/root.go:64-75` checks env only; `.aoa/config arch=off` leaves `aoa arch` registered in help, contradicting `arch.go:3-5`. Duplicated predicate = the T25 drift class recreated; zero CLI-registration coverage in either off mode. | 🟧 | 1 (re-run) | G3/C4 | One shared flag predicate (env + config); integration test asserts `aoa --help` lacks `arch` under BOTH `AOA_ARCH=off` and config `arch=off`, and edges stay zero. **STATUS (F1 re-entry, 2026-07-02): PARTIAL** — predicate unified (`root.go` → `app.ReadArchFlag`), both off modes live-verified dark on the real binary; the required **standing** help-output test still absent (`arch_test.go` tests the predicate only). New residual: `root.go:76-84` reads `.aoa/config` from **cwd**, not project root — the config-off arm is silently skipped when run from a subdirectory. Punch PC5. **STATUS (PC5, 2026-07-03): COVERED/PASSING** — `root.go` walks up from cwd to the nearest `.aoa/` (cwd split-brain fixed); standing help-output tests landed: `TestT36_HelpOutput_ArchAbsentWhenOff` (env_off + config_off) + `TestT36_HelpOutput_ArchPresentByDefault` (commit `2769990`). |
| T37 | **Shutdown flush race** — `doSaveIndexDebounced`/`doFlushEdgeBatch` fire on `time.AfterFunc` goroutines untracked by `a.bgWg`; `Stop` can snapshot clean state and `Store.Close()` mid-flight flush → lost final write / `ErrDatabaseNotOpen`. The C1 refactor removed the mutex fence without a replacement; explicit unlocks (vs defer) also deadlock on mid-section panic. | 🟧 | 1 (re-run) | G0/data-safety | Track flush goroutines (waitgroup or generation flag); test: dirty state + fire timer + immediate `Stop` → final write persisted, no error; restore panic-safe unlock discipline. **STATUS (F1 re-entry, 2026-07-02): PARTIAL** — fix landed (`timerWg` tracks both `AfterFunc` flushes, `app.go:961-965/1021-1024`; `Stop()` drains before `Close`; panic-safe unlock restored); the dedicated dirty-state+Stop persistence test is still absent. **STATUS (F1 light re-verify, 2026-07-03): COVERED/PASSING** — `TestT37_DirtyStateFlushOnStop` landed (`4addf8c`): dirty index + pending edge batch → `Stop()` → reopen bbolt → index + edges both persisted. |
| T38 | **T19 read path unversioned** — `_version` enforced only in `ensureEdgesBucket`; `LoadEdgesForFile`/`LoadAllEdges` ignore it and silently swallow unmarshal errors (`edges.go:163-164, 215-216`) — future-format bucket read-before-write yields silently-empty/zero-value edges (violates "no zero-value edge", G7). | 🟦 | 1 | G4/G7 | Version check on load; unmarshal failure surfaces (log + skip-with-count, never silent zero-values); test reads a future-versioned bucket before any write. **STATUS (F1 re-entry, 2026-07-02): HALF** — `_version` read-path check landed + tested (`TestEdgeStore_T38_VersionCheck_ReadPath`); the surfacing half is still open: unmarshal failures silently swallowed at `edges.go:176,228` (`return nil // corrupt entry`) — corrupt rows still feed silent empties to F2's renderer input. Punch PC4, before Tested. **STATUS (F1 light re-verify, 2026-07-03): COVERED/PASSING** — surfacing half landed (`566689c`): `edges.go:176` propagates corrupt-entry error, `edges.go:228` returns skip-count error; `TestEdgeStore_T38_CorruptEntry_Surfaced` (3 subtests) green. |
| T39 | **T18 assertions weaker than the board AC** — wall-clock "<1s" dropped (`burst_test.go:126-128`); "sub-ms reads" measured against a mock store with the reader stopped pre-flush (never real bbolt during a write tx); a `t.Skip` path passes with zero samples; the 200ms timer path never exercised. | 🟦 | 1 | G0 | Reinstate a bounded wall-clock assertion; measure reads against real bbolt during an in-flight batch tx; fail on zero samples; drive one burst through the actual `AfterFunc` timer. **STATUS (F1 re-entry, 2026-07-02): STILL-GAP — untouched; binds Tested per checkpoint-F1 P5.** All four defects confirmed present at `39bcaab` (`burst_test.go:129` wall-clock dropped, `:205-207` zero-sample `t.Skip`, mock-store reads with the reader stopped pre-flush, timer path never exercised). Punch PC3. **STATUS (PC3, 2026-07-03): COVERED/PASSING** — all four items closed (`d1a4213`): wall-clock <10s bound in `tx_count_bounded`; zero-sample `t.Skip` → `require.NotEmpty`; new `real_bbolt_reads_during_flush` (20 concurrent reads against real bbolt during a write tx, P99 <50ms); new `timer_path` (AfterFunc fires naturally, exactly 1 `SaveEdgesBatch`). 6/6 burst subtests green. |
| T40 | **`make check` lint has never been green** — golangci-lint absent from the machine AND from `.github/workflows/ci.yml`; 170 pre-existing findings at F0 baseline; F1 added 5 (2 unused arch bucket vars — evidence of the missing resolver — 2 test nits, 1 comment; `burst_test.go:75` badLock is an intentional-probe false positive). The L19.13 gate says "make check" but the lint step cannot pass as scoped. | 🟦 | 1 (ruling) | G8/hygiene | Fix the 5 F1 findings now; panel ruling (checkpoint-F1 P6): either burn down/baseline the 170 and add lint to CI, or redefine the gate as vet+test+build and say so in the Makefile. No silent green. **STATUS (F1 re-entry, 2026-07-02): PARTIAL — the 5 F1-introduced findings are fixed** (zero findings in F1-new files, independently verified), **but `make check` still exits 1** on the never-green pre-existing baseline (gate-runner counted 91 baseline-class remaining; red-team measured 170 total = the F0 baseline — a **committed baseline artifact** must settle the count). The P6-1 ruling exists only as prose: no baseline file, no lint-diff target, `Makefile:50` unchanged, no CI lint step. Punch PC2 — record AND mechanize the ruling; gate red until then. Lint output: `details/lint-F1-rerun-2026-07-02.txt`. **STATUS (PC2, 2026-07-03): SATISFIED — `make check` exits 0.** Ruling mechanized (`2a22dcd`): `check` runs `lint-check`, which gates on `golangci-lint --new-from-rev=cd33a5c` (zero F1-introduced findings = PASS; any finding in F1-changed lines = FAIL); baseline artifact `scripts/lint-baseline.txt` committed, count settled (91 visible on HEAD / 170 total at F0 / 0 F1-introduced). Re-verified at `4addf8c` with the diff step visibly executed. |

**Added by the F1 re-entry checkpoint (2026-07-02 — see `checkpoint-F1.md`, re-entry record).**
The full L19.13 re-run per checkpoint-F1 P4: every executable board gate green, including the
P4-widened T1 (**+0.06% p50**, ON arm incl. §2.4 resolve + `ReplaceAllEdges` persist to real
bbolt, 4 alternating interleaved pairs) and the previously-failed L19.9 §2.4 resolver (15/15)
and L19.8 C1 remediation (`-race -count=50` green). Verdict **Conditional** — `make check`
still red at lint pending the mechanized P6 ruling (T40), T38-surfacing/T39 P5-bound hardening
open, T35/T37 dedicated tests absent, and the panel exposed these NEW gaps:

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T41 | **Watcher write path bypasses the §2.4 resolver — mixed `ImportPath` keyspace** — `facts.Resolve` runs only on the bulk paths (`app.go:877-882` WarmCaches, `:3159-3166` Reindex); the watcher (`watcher.go:149-165`) queues parser-**raw** specs into `markEdgeBatchDirty`→`doFlushEdgeBatch`→`SaveEdgesBatch` with no resolve, and unresolved specs are never routed to `facts_unresolved`. Any post-boot edit reverts that file's rows from resolved (`internal/app`, `ext:std/fmt`) to raw (`github.com/corey/aoa/internal/app`, `fmt`) — `LoadAllEdges` (F2's renderer input) becomes a mixed keyspace; T22's "N single-file edits ≡ one full rebuild" is violated by construction. Third instance of the T33/T36 split-brain class; `ports/facts.go:8-10` still (falsely) promises resolution in the EdgeStore write path. Found independently by two panel lenses. | 🟥 | 1 (Tested) | G1/G7 | Resolve at **one write choke-point** (inside `doFlushEdgeBatch`: snapshot → `Resolve` → `SaveEdgesBatch` + `SaveUnresolved`; or in the EdgeStore write path as the port promises). Test: a watcher-persisted edge carries resolved/`ext:` `ImportPath` byte-equal to the bulk path for the same file, and unresolved specs reach `facts_unresolved`; fix the port comment. **STATUS (PC1, 2026-07-03): COVERED/PASSING** — one choke-point landed (`b03ca02`): `doFlushEdgeBatch` snapshots under mu then resolves via `resolveEdgeBatch` before `SaveEdgesBatch`, routes unresolved specs to `SaveUnresolved`; `ports/facts.go` comment corrected; `TestT41_WatcherPersistedEdgesResolved` green (re-verified at `4addf8c`). |
| T42 | **`facts_unresolved` has no reset path** — `ReplaceAllEdges` clears `edges` only (T34); `SaveUnresolved` (`edges.go:275-302`) accumulates forever, so stale unresolved rows from deleted/renamed files survive every rebuild — the T34 phantom-row class on the sibling bucket F2's findings view reads. Untested. | 🟧 | 1 (Tested) | G7 | Rebuild-after-delete/rename test: unresolved rows for dead files are cleared by `ReplaceAllEdges` (or an equivalent replace-all on the bucket); no accumulation across N rebuilds. Fix alongside T41 — same choke-point. **STATUS (PC1, 2026-07-03): COVERED/PASSING** — `ReplaceAllEdges` drops `facts_unresolved` in the same atomic tx (`b03ca02`); `TestEdgeStore_ReplaceAllEdges_ClearsUnresolved` green (re-verified at `4addf8c`). |
| T43 | **Upgrade-boot gap — existing installs boot with an empty edges bucket** — `WarmCaches` derives+persists edges only when `r.FileCount == 0` (`app.go:859-917`); a pre-existing install upgrading to the arch binary boots from the cached index and has zero edges until a manual Reindex or per-file watcher touches. F2 renders "no architecture" for every upgrader. Undocumented, untested. **STATUS (F2, 2026-07-03): COVERED/PASSING as specced** — landed commit-zero on `f2-agent-face`; `TestT43_UpgradeBootEdgeBackfill/{arch_on,arch_off}` green (real bbolt, real parser). Residual: the probe checks `HasEdgesBucket` **only** — the same defect one bucket later (edges present, `arch_shards`/manifest absent) never fires, so an F1-era upgrader still boots with no shards → **T45** (punch PC1, binds Base-2 Tested). | 🟧 | 2 (F2 entry) | G6 | Boot with a populated index + missing/empty edges bucket + flag on → edges derived (one-shot backfill) or the behavior explicitly documented and surfaced; test asserts a non-empty bucket after boot on an upgraded DB. |
| T44 | **>1MB file early-return retains stale edges + index rows** — `watcher.go:103-106` returns before `removeFileFromIndex`/`markEdgeBatchDirty(id, nil)`; a file that grows past the cap keeps serving its old symbols and edges (checkpoint-F1 finding-15 residual, still open). | 🟦 | 1 | G7 | Grow a fixture file past 1MB → its index tokens and edge rows are dropped (or the retention made explicit + stamped); no stale rows served. |

**F1 light re-verify (2026-07-03): Base 1 stamped Tested** (`f1-substrate` @ `4addf8c`). PC1–PC6
landed (`b03ca02` T41+T42 · `2a22dcd` T40 mechanized · `d1a4213` T39 · `566689c`+`4addf8c`
T35/T37/T38 · `2769990` T36/PC5); affected gates re-run green: T41/T42 tests, **`make check`
exit 0** (lint diff step executed against `cd33a5c`, 0 new findings), T39 6/6, T35/T37/T38,
`-race -count=2 internal/app` clean, full suite green, both builds. **Still open after Base 1:**
T43 (binds F2 kickoff — upgrade-boot ruling recorded in checkpoint-F1.md), T44 (hardening),
T30 byte-equality strengthening, T34 app-level reindex-after-shrink e2e.

**Added by the F2 phase checkpoint (2026-07-03 — see `checkpoint-F2.md`).** F2's gated set
was T43/T4/T5/T6/T8/T11/T15/T19-C3/T22/C1/C4 + demo (kickoff-F2 §6, as amended by rulings
R1–R4 in the record); every command executed green (`make check` exit 0, full suite, `-race
-count=50`, live demo transcript), verdict **Conditional** — T8 passed vacuously (zero arch
bytes compared; root cause = the boot-derive gap T45), the findings channel is unfueled
(T46), and the C1-arch harness never reaches the guarded writes (T47). Punch PC1–PC7 binds
Tested (PC8 hygiene binds the merge); light re-verify of the affected gates then stamps
Base 2. The panel exposed these NEW gaps:

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T45 | **Boot-time shard derivation gap** — `deriveArch` fires only from watcher flush (`app.go:1110`), `Reindex` (`:3313`), or the T43 backfill, whose probe checks the **edges bucket only** (`app.go:966`); `aoa init` persists edges but never derives shards (`init.go:130`). Canonical flow `init → daemon start → arch views` returns "no data" indefinitely until a file edit; the T6 helper documents the hole with a workaround double-`init` (`t6_toolkit_test.go:29-33`). Made T8 vacuous; will trip the F3 viewer and the L19.21 stranger-repo gate on first contact. | 🟥 | 2 (Tested) | G6/G3 | Derive when shards/manifest are missing and edges exist (extend the T43 probe to `arch_shards`, or derive-on-daemon-start when the manifest is absent). Test: `init` → daemon start → `arch views` returns data with **no file edit**; remove the T6 double-init workaround. **Punch PC1.** **STATUS (F2 re-verify, 2026-07-03): COVERED/PASSING** @ `e31e853` — `HasArchBucket` probe at end of WarmCaches (mutually exclusive with T43's trigger), `TestT45_*` ×3 green `-race`, T6 double-init workaround removed. |
| T46 | **Dead-candidate findings unfueled — G7 breach in shipped output** — `buildRefHits` returns **nil** with a TODO (`internal/app/arch.go:343-345`), so every zero-inbound unit fires; the message claims "no index references" (`detect.go:385`) — an unverified assertion; fresh T15 run: **30,040 findings for 30,000 units**. `findings --new` has no baseline semantics — it is "any findings exist" (`len(data) > 2`, `arch_findings.go:80` + socket `hasNew`), so as a CI gate it is permanently red on real repos. T5 passes because it tests the domain function, not the wiring. | 🟥 | 2 (Tested) | G7/G6 | Wire refHits from index token hits (or gate the dead-candidate detector off until fueled); message must not claim index evidence while refHits is nil; define + record `--new` baseline semantics (or rename). Test at the app level with a real index. **Punch PC3.** **STATUS (F2 re-verify, 2026-07-03): COVERED/PASSING** @ `d6c523b` — `buildRefHits` real (token refs at unit grain; nil only when index nil); message honest both branches; `--new` = set-difference vs `.aoa/arch/findings-baseline.json` (`--baseline` writes it). Fixture: nil-fuel 30,000 dead-candidates → real-fuel **2**; own repo → 1. (The 30,040 were god-nodes — synthetic-fixture shape, recorded honestly.) |
| T47 | **C1-arch T17 extension half-vacuous** — `TestT17_ArchWritePathNotUnderMu` uses a noopStore whose `LoadAllEdges` returns nil → `deriveArch` early-returns **before any write** (`lockorder_test.go:660-697`, comment admits it); `SaveShards`/`SaveManifest`/`SaveFindings` never actually execute under the lock guard. Code audit is clean (snapshot-release-write verified), but the standing regression doesn't reach the writes. | 🟧 | 2 (Tested) | G4/G8 | Lock-guard store whose `LoadAllEdges` returns real fixture edges so all three arch writes run under the guard; `-race -count=50` stays green. **Punch PC4.** **STATUS (F2 re-verify, 2026-07-03): COVERED/PASSING** @ `e7be2b6` — guarded writes execute; guard liveness proven by negative subtest; `-race -count=50` ok 8.3s. |
| T48 | **C4 socket-level dark probe absent** — no test sends a `MethodArch*` request to a flag-off daemon; the `archQuerier` guard path (`socket/server.go:588-598`) has zero direct coverage (dark checks are CLI-registration + nil-service only). Note: flag **defaults ON** ("dark" is opt-in — the carried F1 "surfaces dark, emission default-ON" ruling); flag-off answer = explicit `"arch not available (C4)"` error, not unknown-method (adopted interpretation, checkpoint-F2 R2). | 🟧 | 2 (Tested) | C4/G3 | Start a flag-off daemon, send each `MethodArch*` request, assert the clear C4 error and that the `server.go:246` default arm stays untouched for non-arch methods. **Punch PC6.** **STATUS (F2 re-verify, 2026-07-03): COVERED/PASSING with recorded deviation** @ `543c743` — socket-package unit test drives `Arch()=nil` (the exact flag-off condition) over a real socket for all six methods + non-arch unaffected; subprocess env propagation to a daemon child was unreliable under the harness, so the probe is unit-level at `server.go:587-598`. |
| T49 | **Protocol surface drift — 5/6 methods + an unadopted verb** — `MethodArchJourney` is a stub returning "not yet implemented" (`server.go:700-706`) while the gate text claims six `MethodArch*`; an `aoa arch pack` subcommand exists outside the adopted six-method/two-alias surface (reach/blast ADR 2026-07-02) — it is L22.5's verb. | 🟧 | 2 (Tested) | G3/G8 | Adjudicate: implement Journey or de-register to five with an ADR note; remove `arch pack` or record the deviation. A test asserts the registered surface matches the adopted ADR. **Punch PC7.** **STATUS (F2 re-verify, 2026-07-03): ADJUDICATED, residual carried** @ `a8a4080` — `arch pack` REMOVED (L22.5's verb, pool law); Journey adjudged **M+** (no port method, no journey data source in F2) — remains the registered sixth method returning an honest not-implemented error, six-method ADR intact; body binds to a named row at next pull. |
| T50 | **Ext-unit provenance empty — "file:line on every node" proven intra-repo only** — live `aggregateEdges` creates ext-target units with `File:""`, `Line:0` (`internal/app/arch.go:149-156`); every real repo's shards contain empty-provenance nodes; no golden covers an ext unit (T4 fixture is all-intra-repo). By construction ext units have no source line — but that must be stamped/asserted, never silent. | 🟦 | 2 | G7 | Golden + property: ext units carry an explicit ext marker (grain honest, no phantom `file:line`), intra-repo units always carry real `file:line`; add an ext-target unit to the T4 fixture. |
| T51 | **Wipe/derive lifecycle residuals (one pre-existing)** — (a) `deriveArch` vs `WipeProject` race: an in-flight derive can recreate the project bucket with stale arch cache post-wipe (`app/arch.go:304-332` vs `app.go:3418-3423`; `archDeriveMu` doesn't cover wipe; cache-only, self-heals); (b) **pre-existing, pre-F2**: `WipeProject` holds `a.mu` across `Store.DeleteProject` (`app.go:3419-3423`, commit `b3a05ac`) — the exact C1 class, carried as logged residual, does not re-open F2; (c) `SaveShards` never prunes superseded hash keys and `DeleteShardsForScope` has no production caller — `arch_shards` grows monotonically in a long-lived daemon (disk-only). | 🟦 | 2/3 | C1/G0 | (a) cover wipe with the derive serialization (or check a wipe generation before write); (b) move `DeleteProject` out from under `a.mu` + extend T17; (c) prune superseded keys on manifest swap or give `DeleteShardsForScope` a caller; tests for each. |

**F2 checkpoint (2026-07-03): Base 2 verdict Conditional** (`f2-agent-face` @ `f12c38b`).
Gates green-as-commanded but T8 vacuous (T45 root cause), findings unfueled (T46), C1-arch
harness short (T47). **Binds Tested:** PC1 (T45) · PC2 (T8 de-vacuation) · PC3 (T46) ·
PC4 (T47) · PC5 (T15 RSS/read halves) · PC6 (T48) · PC7 (T49). Hygiene PC8 (findings port,
triplicated BFS/slug, dead import-keeper) binds the merge to `playbook`, not the stamp.
Rulings recorded in the record: R1 T4 scope (entity/simple shards → L19.23) · R2 C4 socket
wording · R3 demo discharged by live transcript · R4 shim lazy-revive semantics. Carried
non-blocking: T22 incremental-equivalence, T50, T51, T44/T30/T34 from Base 1.

**Added by the F3 phase checkpoint (2026-07-03 — see `checkpoint-F3.md`).** F3's gated set
was the L19.21 row (T10/T9/T16/T20/C4-e2e/`--light`/C3-sweep, kickoff-F3 §6 as ruled by
U-1/U-2) + the L19.19/L19.20/L19.23 AC anchors; verdict **Conditional** on `f3-viewer` @
`5482c87` — most gates green with real output (T10 on the pinned go-cmp repo, T9 3/4, T16
budgets, C4 live-dark incl. the F-6 static subtree, L19.20 refresh at +1s), but the code
view crashes the viewer (T9 1/4 FAIL), the L19.19 byte-parity AC fails, and the T20 soak
was never authored. Punch PF1–PF7 binds Tested (PF8–PF9 hardening); rulings R1 (zero-symbol
304 reading, pending user ratification) / R2 (T-5 pin) / R3 (headless-chromium T9 within
U-2) recorded. The panel exposed these NEW gaps:

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T52 | **C4 kill-switch bypassed at the caching layer** — the shard route is wrapped in `withETag` (`arch_handler.go:78` → `server.go:162-176`), which compares `If-None-Match` against the **global** revision and returns 304 **before** the `archQuerier` fence runs (`arch_handler.go:288-299`). Live with arch disabled: `If-None-Match: <rev>` (rev obtainable from `/api/stats`) → 304 instead of 404 — an existence oracle through the kill switch; fenced 404s also carry an ETag. Invisible to every C4 test because none calls `SetRevisionSource` (withETag was a no-op under test) and the live curl probes sent no `If-None-Match`. | 🟥 | 3 (Tested) | C4/G4 | Fence before ETag (or drop `withETag` on this route — also resolves the competing global-rev vs content-hash cache strategies); test: disabled daemon **with a revision source set** + `If-None-Match` → 404, no ETag header. **Punch PF4.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `7842397` — `withETag` dropped from the shard route; `TestArchC4_ShardWithRevisionSource_DisabledReturns404`: matching If-None-Match + disabled → 404, never 304. |
| T53 | **Immutable-cache poisoning on the shard route** — `handleArchShard` (`arch_handler.go:250-284`) ignores `?v=`, serves current-manifest bytes, yet stamps `Cache-Control: immutable` keyed to the *requested* hash; a derive between manifest fetch and shard fetch caches wrong bytes forever under hash X, and an edit→undo across a 12s poll boundary (the L19.22 demo flow) replays a permanently stale view — defeats L19.20 "no stale". Related: `buildEstatesManifest` re-reads the manifest per `q.View()` call (`app/arch.go:476-488`) — non-atomic snapshot can pair rev_N hashes with rev_N+1 headers. | 🟥 | 3 (Tested) | G6/G1 | Serve by the requested hash (`arch_shards` is already content-addressed `{scope}/{view}@{hash}`) or set `immutable` only when `ContentHash(data)==v`; snapshot the manifest once per synthesis. Test: request an old `?v=` mid-derive → correct bytes or non-immutable response, never wrong-bytes-immutable. **Punch PF5.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `9ffbe57` — shard served by requested hash (`ContentHash(data)==v` gates `immutable`); stale `?v=` → 404 refetch-manifest; tests both paths. |
| T54 | **Code shard breaks the viewer + fabricates edges** — (a) `render_code.go` emits edges without `label`; `viewer.js:350` `lblW=t=>snap(t.length*7+14)` crashes `layoutSimple` → "VIEW LOAD FAILED", 0 elements (the T9 code failure; hits L19.23's AC and T10's U-1 "code if landed" clause); (b) `render_code.go:108-115` connects nodes in **BFS order** ("linear chain"), not actual dep edges — synthetic edges undisclosed by the prov label (G7/litmus). | 🟥 | 3 (Tested) | G6/G7 | Emit `label` on code edges (or guard `lblW`); replace the linear chain with BFS-tree parent→child edges (each a real dep, data already in `codeWalkBFS`) or state edges = walk order in the prov label; re-screenshot + blind-judge the code view. **Punch PF1 + PF6.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `48982a9`+`f190e4e` — `lblW` guarded AND edges carry `label:"imports"`; edges are real BFS-tree import deps; prov label honest; golden regen; re-screenshot blind-judged **PASS** (pre-fix FAIL confirms discrimination). |
| T55 | **CLI/browser shard byte-parity broken and untested-for-real** — `client.call` decodes `Result` as `interface{}` then re-marshals (`socket/client.go:240-247`), destroying `json.RawMessage` byte order (CLI hash `da67b01be192` vs HTTP `aea53105c08e`, semantically equal); R4 lazy-revive makes the byte-clean direct-RO path unreachable; CLI adds a trailing `\n` (`fmt.Println`, `cmd/arch.go:248`); the only test is mock-level and masks the divergence with `strings.TrimSpace` (`arch_handler_test.go:263`). L19.19's AC fails as written. | 🟥 | 3 (Tested) | G3/G1 | Decode `Response.Result` as `json.RawMessage`; record the trailing-newline treatment (drop it or scope the AC to payload bytes); de-mask the TrimSpace; gate evidence = real `aoa arch view X` vs real `curl` byte compare. **Punch PF2.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `b381eba` — `json.RawMessage` end-to-end (16 call sites); CLI writes stored bytes verbatim, no trailing `\n`; TrimSpace de-masked; live xxd CLI==HTTP identical. |
| T56 | **Fork-guard weaker than the T16 spec** — checks only head/tail 512-byte substrings of viewer.js (middle drift passes), `t.Skipf`s when the mockup is absent (green-by-skip in a stranger checkout), never guards index.html/viewer.css (`arch_forkguard_test.go:47-110`). | 🟧 | 3 | G8/maintenance | Full-hash compare of all three extracted files vs generator source; absent mockup = fail (or an explicitly recorded CI-context skip), not silent green. **PF8 (hardening).** |
| T57 | **Estate label hardcoded on stranger repos** — `buildEstatesManifest`/`emptyEstatesManifest` stamp `label:"aOa", tech:"Go"` (`arch_handler.go:140-144, 214-218`) regardless of repo; the T9 go-cmp screenshots render cmp/cmpopts units under an estate labeled "aOa" — the blind judge was shown a mislabeled estate. | 🟧 | 3 (Tested) | G7 | Derive label/tech from the project root (and the index's language facts where available); assert on a non-aOa fixture the manifest never claims "aOa". Fix before re-capturing T9/T10 evidence. **Punch PF7.** **STATUS (F3 re-verify, 2026-07-03): COVERED/PASSING** @ `9ffbe57` — label from project-root basename, tech from index language histogram; non-aOa fixture asserted. |
| T58 | **T15 arch budget evidence excludes the code renderer** — bench + latency test pass `symIndex=nil` (`bench_test.go:142,190`), so the 30k-unit 2.6–2.8s / RSS numbers never exercise `RenderCode`'s symbol walk; the recorded T15 claim is scoped accordingly in checkpoint-F3. | 🟦 | 3 | G0/G8 | Re-run the 30k bench with a populated `symIndex` (or keep the claim permanently scoped); assert the delta stays within the T15 budgets. **PF9 (hardening).** |
| T59 | **C4/lean evidence is live-run-only — no standing regressions** — all web C4 tests build a bespoke mux (`setupArchServer`), never the real `Start()` mux: re-adding `static` wholesale to the base embed re-leaks `static/arch/*` past the kill switch and no test fails (the F-6 case is guarded only by `embed.go`'s 4-file list); the `--light` guard is a 20MB size cap — a leaked 544KB bundle passes 40× over (this run's strings-scan was manual gate evidence, not a test). | 🟧 | 3 | C4/G2 | (a) A real-mux flag-off test asserting `/arch/*` + `/static/arch/*` → 404 while `/` serves; (b) a lean-build probe (strings/nm or `-tags lean` test) asserting the bundle bytes + arch routes are absent. Hardening — the live e2e discharged this phase's gate. |
| T60 | **F3 hygiene grab-bag** — unquoted ETags violate RFC 7232 (brittle behind proxies) + manifest response lacks `Cache-Control`; pre-derive empty manifest carries no ETag → full 200 + complete re-render every 12s poll until first derive (`viewer.js:1028`); `http.NotFound(w, nil)` passes a nil `*http.Request` (`arch_handler.go:290,295`); `/tmp` daemon/socket debris leaked by integration test runs. | 🟦 | 3 | G0/hygiene | Quote ETags + add Cache-Control on the manifest; give the empty manifest an ETag; thread the real request into `archQuerier`; test-suite cleanup of spawned daemons/sockets. |

**F3 checkpoint (2026-07-03): verdict Conditional** (`f3-viewer` @ `5482c87`). **Binds Tested:**
PF1+PF6 (T54) · PF2 (T55) · PF3 (T20 soak) · PF4 (T52) · PF5 (T53) · PF7 (T57). Hardening
carried: PF8 (T56) · PF9 (T58) · T59 · T60. L19.22 demo blocked on L19.21; L22 stays closed.
Record: `checkpoint-F3.md`.

---

## Reading the gaps

- **The blockers cluster at the base gates, exactly where they should.** Base 1 cannot
  pass without T1–T3; Base 2 without T4; Base 3 without T9–T10; Base 4 without T12. Fold
  each into its base's `Tested` gate in `00-INTEGRATION-BOARD.md` §4.
- **T1 is the keystone of the keystone.** If the G0 benchmark fails, the whole sequence
  re-plans (E1 moves off the always-on pass). Run it *first*, before committing build effort.
- **T12 is the one safety test that protects the brand promise** — "the agent suggests,
  never mutates." It is cheap and non-negotiable for any Base-4 feature.
- **The parity→determinism shift (T4, T13) is a strategy change, not just a gap:** the
  arch layer's correctness contract is golden-fixture byte-stability, since there is no
  Python oracle. State this in the test docs so a future contributor doesn't look for
  parity fixtures that will never exist.

**Maintenance-free-for-the-user goal:** T3 (freshness), T10 (incremental correctness),
and T16 (no CDN / offline bundle) are the tests that keep the product *self-maintaining* —
diagrams that never go stale, never need a manual rebuild, and never phone home. They are
the ones to never let regress.

| T61 | **Empty-but-valid edges bucket suppresses backfill forever** — discovered live 2026-07-03 (arch-tab work): a `lean` binary's `aoa init` (no tree-sitter) with arch on writes a schema-valid, EMPTY `edges` bucket; every later standard-binary boot then skips T43 (`HasEdgesBucket`=true) and T45's derive early-returns on 0 edges — the project shows "no views" permanently until a manual wipe+re-init. Same trap when grammars are absent (fresh project pre-`download.sh`, or post-`wipe` which deletes `.so` files). | 🟧 | 3 | G6 | Treat an EMPTY edges bucket (0 keys) as absent in the T43 probe (or re-probe when `LoadAllEdges`=0 and the parser now has grammars); surface "grammars missing — run download.sh" in `arch views`/dashboard instead of silent emptiness; test: lean init → standard boot → backfill fires. |
| T62 | **Daemon outlives its deleted project — zombie accumulation** — user-caught live 2026-07-04: 13 orphaned `/tmp/aoa-integration-*/aoa daemon` processes (binaries + project dirs long deleted) burning CPU for hours. Two halves: (a) test harness: `cli_test.go:59`'s pkill sweep runs only at suite START, so interrupted runs leak until the next run; no `t.Cleanup`-independent guard when the test PROCESS is killed; (b) product: a daemon whose project root (or `.aoa/`) is deleted keeps running indefinitely — any user deleting a project leaves a zombie. | 🟧 | 3 | G0/G6 | (a) suite-END sweep + per-daemon watchdog (daemon exits if its own binary path or project root vanishes — check in the existing health/timer loop, debounced to survive transient unmounts); (b) test asserting a daemon self-terminates within N s of project-root deletion. |

| T63 | **Lock-free live-index reads in peek paths (pre-existing, now two callers)** — socket `handlePeek` (server.go:~536) and the new web `appIndexQuerier` (index_querier.go:90-108) both read `Index.Tokens/Metadata/Files` off the live pointer without `App.mu`, while `Reindex` swaps those fields under mu (app.go:3281-3283). Race detector clean today (reads are map lookups on swapped-in maps), but it is the same struct-field read/write class as the 5980b6a blocker. P1 merge review carried it as punch: harden BOTH callers together (atomic pointer swap of the whole Index or Clone-on-read), never one. | 🟦 | 3 | G0/C1 | Atomic Index pointer (or read-clone) adopted in one change covering socket+web peek; race test that swaps Index under concurrent HTTP peek/refs. |

**Added by the V1-A phase checkpoint (2026-07-14 — see `checkpoint-V1-A.md`).** V1-A's gated
set was the north-star plan §V1-A line (5-second-test screenshots on shorty + gin + one
stranger repo · `make check` · no golden regression except intended fields) + the group-V
binding gates T4/T9/T11/T16/T20 (T1/T15 N/A — no index-time extractor). Verdict
**Conditional** on `feat/arch-role-render` @ `5dd0122`: every executable code gate green,
but the screenshot gate's evidence was captured against **stale pre-phase shards on 2 of 3
labs** (T9 re-opened; root cause T64), the freshness chip masked the staleness (T65), and
the fork-guard is green while certifying ~5% of the file (T56 escalated to binding, punch
PA4). Punch PA1–PA4 binds Tested. The panel exposed these NEW gaps:

| # | Gap | Type | Base | Goal at risk | What the test must do |
|---|-----|------|------|-------------|----------------------|
| T64 | **No shard schema-version — binary upgrade serves mixed-vintage views indefinitely** — `deriveArch` fires only on watcher flush, `Reindex`, or the T43/T45 empty-bucket probes; when shards already exist, a binary upgrade never re-derives and shards carry no format version. Proven live 2026-07-14: shorty served pre-A3 shards (no `inferred`), cobra pre-A1 (no `layer`/`ico`/`inferred`) under a current binary — the viewer degrades silently (`layer=undefined` → uniform hash color, no honesty marker). Same T43/T45/T61 upgrade-boot class, one level up. Corollary: any serving-current probe must check **shard bytes** (e.g. `buckets[0].inferred` present), never viewer.js or the manifest (`findingsClause` is injected at serve time in `buildEstatesManifest`). | 🟥 | V1-A (Tested) | G6/G7 | Stamp a schema/format version into shards+manifest; boot with mismatched-version shards → re-derive with no file edit. Test: old-version shard fixture + new binary boot → fresh shards. **Punch PA2.** |
| T65 | **Freshness chip is serve-time, not derive-time** — `generated.timestamp = time.Now()` at serve (`internal/adapters/web/arch_handler.go:153,232`); viewer (`viewer.js:~1979-1988`) renders "● current · code as of now" on every page load regardless of shard age — the fake-live claim the owner ruling forbade; T64's stale labs displayed it over week-old-vintage shards, which is what made the invalid gate evidence undetectable. Manifest stores no derivedAt. | 🟧 | V1-A (Tested) | G7/G6 | Stamp derivedAt at derive time in the manifest; chip computes age from it; test asserts the manifest timestamp is stable across serves and changes only on re-derive. Full state machine ("change detected — updating…") = V1-B B3. **Punch PA3.** |
| T66 | **`graph` endpoint three-mode divergence** — daemon-path `Graph()` folds MIXED learned edges + domain enrichment (`internal/app/arch.go:660 mergeLearnedEdges`, Seam-C — rode the branch un-gated); the CLI direct-RO path (`cmd/aoa/cmd/arch.go:237-259`) calls `BuildGraphPayload(edges, nil, ...)` with no learned edges → same command, different mode, different semantics. T8's byte-parity suite covers views/findings/facts/derive, not graph. Per-edge `prov:"mixed"` is stamped (leash-honest), but the G3 parity law is breached untested. | 🟧 | V1-B / Seam-C checkpoint | G3/G1 | Extend the T8 byte-parity suite to `graph` across daemon / direct-RO / dead-daemon (learned-edge fold identical or the divergence explicitly stamped per mode). Gate at the self-learning/Seam-C checkpoint or V1-B, whichever lands first. **Carried PA5.** |
| T67 | **`caption.go` doc/code mismatch — latent A3 break for table views** — the DeriveCaption doc comment (`internal/domain/arch/caption.go:16-19`) claims the table kind returns a calm `"N rows"` caption with the flagged fragment in `findingsClause`, and that matrix/entity/simple return clause `""`; the code does neither (`deriveTableCaption` bakes `" · ⚠ %d flagged"` into the calm caption at `:119-122`, and all kinds return the findings suffix as the clause at `:171-191`). No production path hits it (RenderCycles builds its count manually), but the first table shard routed through DeriveCaption ships a ⚠ glyph in the calm caption — `TestRenderComponent_CalmCaptionNeverMentionsFindings` guards buckets only. | 🟦 | V1-C entry | G8/A3-ruling | Align code to doc (move the flagged fragment into the clause for all kinds) or doc to code — recorded either way; extend the calm-caption invariant test to every shard kind. Fix before V1-C adds table views. **Carried PA6.** |

**V1-A checkpoint (2026-07-14): verdict Conditional** (`feat/arch-role-render` @ `5dd0122`).
**Binds Tested:** PA1 (T9 honest re-capture: labs re-derived, shard-byte probe, localStorage
cleared, all live views, true stranger repo, blind-judge before owner) · PA2 (T64) · PA3 (T65) ·
PA4 (T56 fork-guard). Carried: PA5 (T66) · PA6 (T67) · PA7 (flat `arch views` findings signal
for agents) · PA8 (header caption de-slug — precondition folded into PA1) · PA9 (governance:
tasks #15/#17 adjudication, kickoff discipline for V1-B). Record: `checkpoint-V1-A.md`.
