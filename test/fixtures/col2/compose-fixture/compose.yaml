services:
  api:
    build: .
    image: fixture/api:1.0
    ports:
      - "8080:8080"
    depends_on:
      - db
      - cache
  db:
    image: postgres:16
    ports:
      - 5432
  cache:
    image: redis:7
